{{ ansible_managed | comment }}
[Service]
Environment="HTTP_PROXY={{ docker_proxy_env.http_proxy }}"
Environment="HTTPS_PROXY={{ docker_proxy_env.https_proxy }}"
Environment=NO_PROXY="{{ docker_proxy_env.no_proxy }}"
