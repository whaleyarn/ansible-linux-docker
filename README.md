# Docker

### Role Variables

| Variable                   | Default   | Description                                                                 |
| :------------------------- | :-------- | :-------------------------------------------------------------------------- |
| `docker_apt_mirror_region` | `default` | Sets the docker APT mirror. Options: `default` (official), `cn` (Tsinghua), |
